import { Box, Typography } from "@mui/material";

function UserRolesTable() {
   return(
    <Box>
        <Typography>
            in user
        </Typography>
    </Box>
   );
};

export default UserRolesTable;